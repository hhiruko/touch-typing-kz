import lsns from '.';

const storyList = [
  lsns.STORY1,
  // lsns.STORY2,
  // lsns.STORY3,
  // lsns.STORY4,
  // lsns.STORY5.replace(/[\t\n]/g, ' '),
  // lsns.STORY6.replace(/[\t\n]/g, ' '),
  // lsns.STORY7.replace(/[\t\n]/g, ' '),
  // lsns.STORY8,
  // lsns.STORY9,
  // lsns.STORY10,
];

export default storyList;
